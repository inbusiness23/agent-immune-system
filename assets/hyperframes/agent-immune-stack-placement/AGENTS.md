# HyperFrames Composition Instructions

Follow the repository `DESIGN.md` and `launch/video-asset-brief.md`.

This composition is the launch candidate. It must show:

- "Your agent rules are making you slower."
- Adding rules creates prompt scar tissue.
- Agent Immune System subtracts, demotes, archives, and regenerates instead of only adding.
- The tool fits above the developer-agent loop.
- It works across Claude Code, Codex, Cursor, and terminal workflows.
- The 40% faster result is scoped to the included deterministic demo fixture.
